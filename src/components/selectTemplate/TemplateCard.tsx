import { TEMPLATE_INFO } from "pages/SelectingTemplatePage";

type TemplatePropsType = {
  temInfo: TEMPLATE_INFO;
};

export const TemplateCard = ({ temInfo }: TemplatePropsType) => {
  return <div>this is card</div>;
};
